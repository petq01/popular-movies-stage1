<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.popmoviessecond">

    <uses-permission android:name="android.permission.INTERNET" />

    <permission android:name="com.android.popmoviessecond.room.provider.READ_WRITE"/>
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <action android:name="android.intent.action.VIEW"/>

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--<provider-->
            <!--android:name=".room.provider.SampleContentProvider"-->
            <!--android:authorities="com.android.popmoviessecond"-->
            <!--android:exported="true"-->
            <!--android:permission="com.android.popmoviessecond.room.provider.READ_WRITE"/>-->
        <!--<provider android:name=".sqllite.MovieProvider" android:exported="true"-->
            <!--android:readPermission="true" android:writePermission="true"-->
            <!--android:authorities="com.android.popmoviessecond" />-->
        <provider
            android:authorities="com.android.popmoviessecond"
            android:name=".room.provider.SampleContentProvider" android:exported="true"
            android:protectionLevel="signature"
            android:readPermission="true" android:writePermission="true"/>
    </application>

</manifest>